#include <stdio.h>
/**
 * main - Print the alphabet in reverse order in lowercase
 * Return: 0 (Success)
 */
int main(void)
{
	char ci;
	for (c = 'z'; c >= 'a'; c--)
		
	{
		putchar(c);
	}
		putchar('\n');
		return (0);
}

